/* package com.example.demo.UI;

import com.example.demo.Core.Badge;

import javax.swing.*;

public class BadgeView {
    private final Badge badge; // Das Badge-Objekt
    private final ImageIcon icon; // Das Icon für das Badge

    // Konstruktor
    public BadgeView(Badge badge) {
        this.badge = badge;
        
        // Wir gehen davon aus, dass das Badge das Bild selbst lädt (über den Badge-Objekt)
        this.icon = badge.getIcon(); // Das Badge hat jetzt ein ImageIcon-Objekt
    }

    // Getter für das Badge
    public Badge getBadge() { 
        return badge; 
    }

    // Getter für das Badge-Icon
    public ImageIcon getIcon() { 
        return icon; 
    }
}
 */