/* package com.example.demo.Data;

import java.io.File;

public class FileManager {

    public static File saveFile(File file, String destinationPath) {
        // Speichere die Datei am Zielort
        File savedFile = new File(destinationPath);
        // Hier Logik zum Speichern oder Verschieben der Datei
        return savedFile;
    }

    public static File loadFile(String filePath) {
        // Lade die Datei von dem angegebenen Pfad
        return new File(filePath);
    }
}
 */